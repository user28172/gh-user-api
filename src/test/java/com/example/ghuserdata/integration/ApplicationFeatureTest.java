package com.example.ghuserdata.integration;

import com.example.ghuserdata.integration.configuration.IntegrationTest;
import org.junit.jupiter.api.Test;

@IntegrationTest
public class ApplicationFeatureTest {

    @Test
    void shouldFetchGithubUserDataAndIncrementCounter() {
        //given user exists in github API
        //given counter for user does not exist

        //when fetching user data via application API

        //then data are correct
        //and counter has been created (=1)

        //when fetching user data again

        //then counter has been incremented (=2)
    }
}
